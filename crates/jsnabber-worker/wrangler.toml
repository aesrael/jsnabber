name = "jsnabber-worker"
main = "build/worker/shim.mjs"
compatibility_date = "2024-01-01"

[build]
command = "export PATH=$PATH:$HOME/.cargo/bin && cargo install -q worker-build && worker-build --release"

[[rules]]
globs = ["**/*.wasm"]
type = "CompiledWasm"
